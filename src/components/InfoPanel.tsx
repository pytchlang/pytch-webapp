import React from "react";

const InfoPanel = () => {
    return (
        <div className="InfoPanel">
            Assets / StdOut / Errors / Tutorial
        </div>
    )
};

export default InfoPanel;
